version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: mysql_finpro_pbo
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db_popculture
    ports:
      - "3306:3306"
    volumes:
      - mysql_finpro_data:/var/lib/mysql

volumes:
  mysql_finpro_data:
